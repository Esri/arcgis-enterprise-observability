This repository contains resources related to configuring observability capabilities for ArcGIS Enterprise. It consists of the following types of resources:

- Guides: Deployment guides for Prometheus and Grafana
- `prometheus-config.yml`: an example configuration to discover and add ArcGIS Server metrics as a scrape target for an existing Prometheus deployment.
- DashboardTempates: Example `.json` files for Grafana Dashboard configurations.